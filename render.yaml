services:
  - type: web
    name: booking-alert-api
    env: python
    plan: free
    buildCommand: ""
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
    autoDeploy: true
    build:
      pythonVersion: 3.11.8
